import { Scene } from '@antv/l7';
import { adcodeType, IDrillDownOption } from './interface';
export default class DrillDownLayer {
    private options;
    private cityLayer;
    private countyLayer;
    private provinceLayer;
    private scene;
    private drillState;
    private layers;
    constructor(scene: Scene, option: Partial<IDrillDownOption>);
    getDefaultOption(): {
        drillDepth: number;
        customTrigger: boolean;
        drillDownTriggerEvent: string;
        drillUpTriggerEvent: string;
        provinceData: never[];
        cityData: never[];
        countyData: never[];
        city: {
            adcode: never[];
        };
        county: {
            adcode: never[];
        };
    };
    addCountryEvent(): void;
    addProvinceEvent(): void;
    addCityEvent(): void;
    show(): void;
    hide(): void;
    destroy(): void;
    showProvinceView(adcode: adcodeType, newData?: Array<{
        [key: string]: any;
    }>, joinByField?: [string, string]): void;
    showCityView(code: adcodeType, newData?: Array<{
        [key: string]: any;
    }>, joinByField?: [string, string]): void;
    /**
     * 向上
     */
    drillUp(): void;
    drillDown(adcode: adcodeType, newData?: Array<{
        [key: string]: any;
    }>, joinByField?: [string, string]): void;
    updateData(layer: 'province' | 'city' | 'county', newData: Array<{
        [key: string]: any;
    }>, joinByField?: [string, string]): void;
    private getLayerOption;
}
